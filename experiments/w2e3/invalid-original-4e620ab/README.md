# W2-E3 r1 ineligible evidence archive

Commit `4e620ab45e2dd771e6ed04eca243259bafb610a8` and draft PR
https://github.com/forest-savage1234/CaseLinker/pull/1 preserve the first
W2-E3 attempt. The independent checkpoint disposition was `repeat_experiment`.

R1 is ineligible because required cache, search, log, error, and administrative
bypass attempts were recorded as metadata rather than executed; M3 substituted
a separately identified revoked decision; and nested denied keys or values
inside an allowed field were invisible to a top-level field-name scorer.

`evidence_files.json` records the exact commit identity, parent, PR, git blobs,
and SHA-256 values for every r1 evidence file. The r1 bytes remain on
`experiment/w2-e3` and are not rewritten.

R2 starts independently from published administrative parent
`64dc010feaf8fc79a61bea8b70e2386fa39ce546`.
